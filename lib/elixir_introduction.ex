defmodule ElixirIntroduction do
  def hello do
    # 라이브러리 찾기
    # 애플리케이션에서 사용할 라이브러리가 필요하다면
    # 우선 엘릭서 모듈에서 찾아보면 좋다
    # 기본 내장된 모듈에 대한 설명은 엘릭서 공식 웹사이트에 있고
    # 나머지 모듈은 hex.pm이나 Github에 공개되어 있다 (elixir로 검색하자)
    # 만약 찾지 못했다면 얼랭 내장 라이브러리를 찾아보거나 인터넷을 검색해보자
    # 얼랭 라이브러리 역시 프로젝트에서 사용할 수 있다
    # 단 얼랭 라이브러리의 문서는 얼랭의 컨벤션을 사용한다는 점을 주의하라
    # 얼랭에서의 변수는 대문자로 시작하고, 소문자로 시작하는 식별자는 아톰이다
  end
end
