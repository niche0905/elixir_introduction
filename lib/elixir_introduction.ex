defmodule ElixirIntroduction do
  def hello do
    # 모듈 지시자
    # 엘릭서에서 다른 모듈을 사용할 때 세가지 지시자를 사용한다
    # 셋 모두 프로그램이 시작하면 함께 실행되며 문법적 스코프를 가진다
    # 자시자의 효과는 지시자를 만난 곳부터 코드를 감싸는 블록이 끝날 때까지 유효하다
  end
end
